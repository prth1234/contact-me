/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'api.pikwy.com',
      'aceternity.com',
      'i.ibb.co',
      // Add other domains as needed
    ],
  },
};


export default nextConfig;
